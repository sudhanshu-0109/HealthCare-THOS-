VITE_API_URL=http://localhost:5000/api
VITE_GOOGLE_CLIENT_ID=
VITE_RAZORPAY_KEY_ID=
VITE_CLOUDINARY_CLOUD_NAME=
# Map tiles for the live ambulance-tracking map are served by OpenFreeMap
# (https://openfreemap.org) via MapLibre GL — free and keyless, no API key needed.
# Optionally override the vector style URL (e.g. self-hosted tiles):
VITE_MAP_STYLE_URL=
